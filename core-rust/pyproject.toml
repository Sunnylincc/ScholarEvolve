[build-system]
requires = ["maturin>=1.8,<2.0"]
build-backend = "maturin"

[project]
name = "scholarevolve-core"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "scholarevolve_core"
python-source = "python"
